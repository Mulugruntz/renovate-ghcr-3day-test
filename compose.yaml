# Manager: docker-compose (default patterns: (^|/)(?:docker-)?compose[^/]*\.ya?ml$)
# Same 'docker' datasource as Dockerfiles, so the ghcr.io/** rule applies here too
# UNLESS you scope the rule with matchManagers: ["dockerfile"].
services:
  uv:
    image: ghcr.io/astral-sh/uv:0.12.7-trixie-slim@sha256:92d38da241c7962f8f863e288cc1c39795b79b6553245f623a82db6be95bdae0
    command: ["uv", "--version"]
