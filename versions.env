# Plain env file, no built-in manager. Picked up by the customManagers regex
# block in renovate.json via the annotation comment below.
# renovate: datasource=docker depName=ghcr.io/astral-sh/uv versioning=docker
UV_VERSION=0.12.7-trixie-slim
