# CASE 2 — FROM with --platform flag  (SUPPORTED: fromRegex consumes '--platform=\S+')
# Expect: same as case 1. The BUILDPLATFORM variable must NOT confuse the extractor.
FROM --platform=$BUILDPLATFORM ghcr.io/astral-sh/uv:0.12.7-trixie-slim@sha256:92d38da241c7962f8f863e288cc1c39795b79b6553245f623a82db6be95bdae0 AS uvsrc
RUN uv --version

# CASE 2b — stage alias reference, NOT an image (SUPPORTED as a skip)
FROM debian:trixie-slim
COPY --from=uvsrc /usr/local/bin/uv /usr/local/bin/uv
