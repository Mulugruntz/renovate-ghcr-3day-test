# CASE 5 — ARG holding the whole image ref, used by FROM  (SUPPORTED)
# extract.ts resolves FROM variables from the 'args' map. The ARG must appear
# BEFORE the FROM (args are collected as instructions are walked in order).
# NOTE: Renovate rewrites the ARG line, not the FROM line.
ARG UV_BASE_IMAGE=astral/uv:0.12.14-trixie-slim@sha256:45d66bb5a1027a9026e35743755860fb888b4b0a19fb0ba3e31e31b094005afe
FROM ${UV_BASE_IMAGE}
RUN uv --version
